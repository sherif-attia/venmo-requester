name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        id: uv
        uses: astral-sh/setup-uv@v5
        with:
          version: latest
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: .python-version

      - name: Install dependencies
        run: uv sync

      - name: Cache Python environment
        uses: actions/cache@v4
        with:
          path: |
            .venv
            __pycache__
            ${{ steps.uv.outputs.uv-path }}
          key: ${{ runner.os }}-python-${{ hashFiles('**/uv.lock') }}

  lint:
    needs: [setup]
    runs-on: ubuntu-latest
    steps:
      - name: Restore Python environment
        uses: actions/cache@v4
        with:
          path: |
            .venv
            __pycache__
            ${{ steps.uv.outputs.uv-path }}
          key: ${{ runner.os }}-python-${{ hashFiles('**/uv.lock') }}

      - name: Lint code
        run: |
          ${{ steps.uv.outputs.uv-path }} run ruff check --output-format=github

  test:
    needs: [setup, lint]
    runs-on: ubuntu-latest
    steps:
      - name: Restore Python environment
        uses: actions/cache@v4
        with:
          path: |
            .venv
            __pycache__
            ${{ steps.uv.outputs.uv-path }}
          key: ${{ runner.os }}-python-${{ hashFiles('**/uv.lock') }}

      - name: Run tests
        run: |
          ${{ steps.uv.outputs.uv-path }} run pytest
